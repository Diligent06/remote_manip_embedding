{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build",
      "type": "shell",
      "command": "make",
      "options": {
        "env": {
          "PATH": "/Applications/ARM/bin:${env:PATH}"
        }
      },
      "group": "build"
    }
  ]
}
